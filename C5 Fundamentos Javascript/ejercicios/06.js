// Resuelve el siguiente problema matemático.
// Reemplaza el valor de null por el correspondiente.

const resto = (21%5);
const nuevoModulo = 21 % 5 === resto;

console.log(resto);

module.exports = nuevoModulo;
